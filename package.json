{
  "name": "swiftshare",
  "version": "1.0.0",
  "private": true,
  "description": "Share Anything. Instantly. - Enterprise-grade File Sharing Platform",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npx concurrently \"npm run dev --workspace=services/backend\" \"npm run dev --workspace=apps/web\"",
    "build": "npm run build --workspaces",
    "test": "jest",
    "lint": "eslint . --ext .ts,.tsx"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
